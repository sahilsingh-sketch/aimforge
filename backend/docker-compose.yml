version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: aimforge_db
    environment:
      POSTGRES_USER: aimforge_user
      POSTGRES_PASSWORD: aimforge_password
      POSTGRES_DB: aimforge_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: aimforge_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  postgres_data:
